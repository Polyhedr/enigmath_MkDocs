local
```
mkdocs serve
```

remote
```
git remote add origin https://github.com/Polyhedr/enigmath.git
git add .; git commit -m "Initial commit"; git branch -M main; git push -u origin main; mkdocs gh-deploy --clean
```